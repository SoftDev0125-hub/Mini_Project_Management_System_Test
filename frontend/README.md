# Frontend (Vite + React + TypeScript)

This folder contains a minimal React + TypeScript scaffold with TailwindCSS and Apollo Client.

Setup:

```powershell
cd frontend
npm install
npm run dev
```

The app expects a GraphQL endpoint configured via `.env` variable `VITE_GRAPHQL_ENDPOINT` (defaults to http://localhost:8000/graphql/).
